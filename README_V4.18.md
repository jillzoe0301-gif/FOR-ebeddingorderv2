# V4.18 GitHub 更新包

解壓縮至專案根目錄後執行：

git add index.html package.json package-lock.json CHANGELOG_V4.18.md
git commit -m "Change sidebar to top text navigation V4.18"
git pull --rebase origin main
git push origin main
